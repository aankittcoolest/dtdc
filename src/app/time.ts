export class Time {
    selectedTime : string
    selectedDay: string

    constructor(selectedDay, selectedTime) {
        this.selectedDay = selectedDay
        this.selectedTime = selectedTime
    }
}
